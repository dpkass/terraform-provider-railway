---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "railway_volume Resource - terraform-provider-railway"
subcategory: ""
description: |-
  Manages only the project-level identity of a Railway persistent volume. Creation does not deploy the volume to any environment. Use railway_volume_instance to create each environment deployment and optionally mount it to a service.
---

# railway_volume (Resource)

Manages only the project-level identity of a Railway persistent volume. Creation does not deploy the volume to any environment. Use `railway_volume_instance` to create each environment deployment and optionally mount it to a service.

## Example Usage

```terraform
resource "railway_volume" "data" {
  project_id = railway_project.example.id
  name       = "application-data"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Display name of the volume.
- `project_id` (String) Identifier of the project that owns the volume. Changing this value replaces the resource.

### Read-Only

- `id` (String) Identifier of the volume.

## Import

Import is supported using the following syntax:

```shell
terraform import railway_volume.data project_id:volume_id
```
