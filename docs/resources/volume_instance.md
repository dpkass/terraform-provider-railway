---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "railway_volume_instance Resource - terraform-provider-railway"
subcategory: ""
description: |-
  Manages one environment deployment of a project-level Railway volume and its optional service mount. Deleting or replacing this resource destroys the environment deployment and its data. Data is not transferred when volume_id or environment_id changes.
---

# railway_volume_instance (Resource)

Manages one environment deployment of a project-level Railway volume and its optional service mount. Deleting or replacing this resource destroys the environment deployment and its data. Data is not transferred when `volume_id` or `environment_id` changes.

## Example Usage

```terraform
resource "railway_volume_instance" "data" {
  volume_id      = railway_volume.data.id
  environment_id = railway_environment.production.id
  size_mb        = 5000
  service_id     = railway_service.api.id
  mount_path     = "/app/data"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `environment_id` (String) Identifier of the environment containing the volume instance. Changing this value replaces the resource, destroying the old environment deployment and its data without transferring it.
- `size_mb` (Number) Provisioned size of the volume instance in megabytes. Updates may increase this value but cannot decrease it.
- `volume_id` (String) Identifier of the project-level volume. Changing this value replaces the resource, destroying this environment deployment and its data without transferring it.

### Optional

- `mount_path` (String) Absolute path where the volume is mounted in the service container. Required when `service_id` is set and must be omitted otherwise.
- `service_id` (String) Identifier of the service to mount the volume on. Omit to keep the environment volume instance detached.

### Read-Only

- `id` (String) Identifier of the environment-specific volume instance.

## Import

Import is supported using the following syntax:

```shell
terraform import railway_volume_instance.data environment_id:volume_id
```
